# The Analog Thing Simulator

This repository is the dependency-free JavaScript unofficial approximate simulator of [**The Analog Thing**](https://the-analog-thing.org/).

No npm install, build step, package manager, network service, or external runtime dependency is required. Open `public/index.html` directly in a browser or serve the repository root with any static HTTP server.
